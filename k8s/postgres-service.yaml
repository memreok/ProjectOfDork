apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: ClusterIP # Dış dünyadan erişimi engeller, sadece K8s içi erişim sağlar (Ücretsizdir)
  selector:
    app: postgres-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432